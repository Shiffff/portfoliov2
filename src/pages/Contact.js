import React from "react";

const Contact = () => {
  const accolade = "{   }";

  return (
    <div className="contactPage">
      <h2 className="pageTitle">
        CONTACT <span className="pointTitle nexttitle">{accolade}</span>
      </h2>
    </div>
  );
};

export default Contact;
